<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Buguersys" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Buguersys" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <main role="main">
      <%= yield %>
    </main>

    <%= javascript_include_tag "application" %>
  </body>

</html>
